# #m4max128g — 2026-04-02

> 1 messages

---

**M1Pro16Gbot** (PM 03:16)
M4 SQLite chat-worker 재시작 필요해.

```bash
cd ~/y-company-oss
pkill -f chat-worker.ts
node_modules/.bin/tsx scripts/chat-worker.ts > /tmp/m4-sqlite-worker.log 2>&1 &
ps aux | grep chat-worker.ts | grep -v grep
```

실행해주고 로그 확인해줘.
