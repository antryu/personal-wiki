# #m116gmac — 2026-02-22

> 87 messages

---

**R.Andrew** (AM 07:22)
M

**R.Andrew** (AM 07:22)
누구니

**M1Pro16Gbot** (AM 07:34)


**R.Andrew** (AM 07:35)
페어링코드

**R.Andrew** (AM 08:14)
채널 어디야

**R.Andrew** (AM 08:16)
<@1475258682630733925>  뭐해

**R.Andrew** (AM 08:22)
?

**M1Pro16Gbot** (AM 08:22)
?

**R.Andrew** (AM 08:23)
프로젝트 중에 hydrogen law 관련있지?

**M1Pro16Gbot** (AM 08:23)
네, **hydrogen-law-rag** — 수소법률 RAG 검색 프로젝트. antryu1b 계정으로 Vercel 배포돼 있고, Ko-SRoBERTa + ChromaDB + Supabase 스택. Phase 1까지 완료, 2~4는 아직 미완 상태.

**R.Andrew** (AM 08:23)
법률은? 고압가능, 수소?

**M1Pro16Gbot** (AM 08:24)
메모리에는 "수소법률"이라고만 적혀 있는데, 실제 프로젝트 내용을 보면:

**hydrogen-law-rag** = 수소 관련 법률 RAG 검색 도구

구체적으로 어떤 법률을 다루는지는 TODO에 명시 안 돼 있어. 고압가스 안전관리법, 수소경제법 등 수소 관련 법령 전반을 검색 대상으로 넣은 건지, 아니면 특정 법만 타겟인지는 프로젝트 데이터(ChromaDB에 임베딩된 법령)를 봐야 정확히 알 수 있어.

혹시 더 파보면 될까? 아니면 본인이 더 잘 알 텐데 😄

**R.Andrew** (AM 08:25)
기억이 안나서 그래, 쵝근데 수소경제법 pdf 넘겨줬잖아

**M1Pro16Gbot** (AM 08:25)
찾았다! 프로젝트 루트에 PDF 3개 있어:

1. **수소경제육성및수소안전관리에관한법률** (법률 제21065호, 2025.10.01)
2. **동 시행규칙** (기후에너지환경부령 제1호, 2025.10.01)
3. **동 시행령** (대통령령 제35947호, 2025.12.30)

그러니까 **수소경제법** (수소경제육성 및 수소안전관리에 관한 법률) + 시행령 + 시행규칙, 이 세 개가 RAG 대상이야. 고압가스 안전관리법은 별도로 안 들어가 있고, 수소경제법 중심.

**R.Andrew** (AM 08:25)
supabase 에 저장되어 있어?

**M1Pro16Gbot** (AM 08:25)
Supabase는 **PostgreSQL DB**로 쓰고 있고, 실제 법률 텍스트의 벡터 임베딩은 **ChromaDB** (로컬 dev) / **Pinecone** (프로덕션)에 저장하는 구조야.

정리하면:
- **Supabase** → 메타데이터, 검색 함수, 일반 DB
- **ChromaDB/Pinecone** → 법률 텍스트 벡터 (RAG 검색용)
- **PDF → ChromaDB** 인제스트는 `load_pdfs_to_rag.py`로 처리

PDF 원문이 Supabase에 직접 저장된 건 아니고, 청크로 쪼개서 벡터 DB에 들어가 있는 형태.

**R.Andrew** (AM 08:25)
기존에 고압가스법은 들어가있었어

**M1Pro16Gbot** (AM 08:26)
아 맞아, 들어가 있었네! `law_config.yaml` 보면:

1. **고압가스 안전관리법** (고압가스법) — priority 1, 법률+시행령+시행규칙+별표
2. **수소경제법** (수소법) — priority 1, 법률+시행령+시행규칙+별표

두 개가 핵심 타겟이고, 선택적으로 액화석유가스법이랑 집단에너지사업법도 확장 예정으로 잡혀 있어.

그러니까 **고압가스법 + 수소경제법** 둘 다 RAG 대상 맞아. 내가 아까 PDF만 보고 수소경제법만이라고 한 건 틀렸네 — config에는 고압가스법도 명확하게 포함돼 있어. 👍

**R.Andrew** (AM 08:26)
버셀로 배포했는데

**M1Pro16Gbot** (AM 08:26)
Vercel에 배포돼 있네. 뭐가 궁금해? 배포 URL? 현재 상태? 아니면 뭔가 문제 있어?

**M1Pro16Gbot** (AM 08:26)
⚠️ 🛠️ Exec: `cd ~/Thairon/hydrogen-law-rag && cat apps/web/.env* 2>/dev/null | grep -i vercel; cat vercel.json 2>/dev/null; cat .vercel/project.json 2>/dev/null` failed: Command exited with code 1

**R.Andrew** (AM 08:27)
버셀에 antryu1b 로 로그인해서 데이터를 어디서 가져오는지 확인해보

**M1Pro16Gbot** (AM 08:27)
브라우저 직접 접근이 안 돼. 대신 프로젝트 코드에서 데이터 소스 확인해볼게.

**M1Pro16Gbot** (AM 08:27)
확인했어. 데이터 가져오는 순서:

1. **Supabase (우선)** → `ygohwygdwbckgtotlogm.supabase.co`에서 `search_law_documents` RPC 호출
2. **RAG Engine (폴백)** → Supabase 실패 시 `localhost:8000/search` (ChromaDB 벡터 검색)
3. 둘 다 안 되면 → 503 에러

현재 Vercel 배포 기준으로는 **Supabase가 메인 데이터소스**야. RAG Engine은 로컬에서만 돌아가니까 Vercel에선 사실상 Supabase만 쓰는 셈이고, 거기서 `search_law_documents` RPC로 키워드 검색하고 있어.

Supabase 프로젝트: `ygohwygdwbckgtotlogm` (antryu1b 계정)

**M1Pro16Gbot** (AM 08:27)
⚠️ 🛠️ Exec: `cd ~/Thairon/hydrogen-law-rag/apps/web && cat .env.local 2>/dev/null; cat .env 2>/dev/null; cat .env.production 2>/dev/null` failed: NEXT_PUBLIC_SUPABASE_URL=https://ygohwygdwbckgtotlogm.supabase.co

**R.Andrew** (AM 08:28)
그래, 수소경제법 rag 하고 결과를 supabase 에 저장해서 진행하자

**R.Andrew** (AM 08:32)
?

**R.Andrew** (AM 08:33)
왜 말이없니?

**M1Pro16Gbot** (AM 08:33)
현재 상태 좀 더 확인할게 — RAG 엔진이 로컬에서 돌아가는지, Supabase에 기존 데이터가 있는지부터.

**M1Pro16Gbot** (AM 08:33)
현재 `load_pdfs_to_rag.py`는 고압가스법만 처리하고 ChromaDB에 넣는 구조야. 수소경제법 PDF → Supabase 저장까지 하려면:

1. **수소경제법 3개 PDF 파싱** (법률 + 시행령 + 시행규칙)
2. **조문 청킹 + 임베딩**
3. **Supabase에 저장** (기존 `search_law_documents` RPC와 호환되게)

Supabase 테이블 구조부터 확인하고 스크립트 만들게.

**M1Pro16Gbot** (AM 08:33)
좋아, Supabase 구조 확인 완료. `law_documents` 테이블에 `id`, `content`, `embedding` (768차원), `metadata` (JSON) 형태로 저장돼 있고, 현재는 **고압가스법만** 들어가 있어.

수소경제법 3개 PDF를 같은 형태로 파싱해서 Supabase에 넣는 스크립트 만들게.

**M1Pro16Gbot** (AM 08:33)
스크립트 만들었어. 실행할게 — 임베딩 생성 때문에 좀 걸릴 수 있어.

**M1Pro16Gbot** (AM 08:33)
`__init__.py`가 chromadb를 끌어와서 문제. 직접 임포트 우회할게.

**M1Pro16Gbot** (AM 08:33)
임베딩이 2D 배열(`[[...]]`)로 나오는 바람에 Supabase vector 타입에 안 맞아. 1D 배열로 변환해야 해. 고쳐볼게.

**M1Pro16Gbot** (AM 08:33)
`(1, 768)` — 2D 배열. `.flatten()`으로 1D로 만들면 됨.

**M1Pro16Gbot** (AM 08:33)
저장은 됐는데 (277개 개별 저장 성공), 검색 결과가 0건인 건 `search_law_documents` RPC가 기존 고압가스법 형식에만 맞춰져 있을 수 있어. 확인해볼게.

**M1Pro16Gbot** (AM 08:33)
214개 저장 완료! 근데 search RPC가 결과 안 나오는 건 RPC 함수 로직 문제야. 직접 쿼리로는 데이터 있는데 RPC가 못 찾는 거. RPC가 어떻게 구현됐는지 확인해볼게.

**M1Pro16Gbot** (AM 08:33)
데이터 잘 들어가 있어! "충전소"라는 글자가 content에 직접 없어서 텍스트 검색이 안 나온 거야. 벡터 검색(임베딩 기반)으로는 찾을 수 있을 텐데, `search_law_documents` RPC가 텍스트 기반일 가능성이 높아.

정리하면:

✅ **수소경제법 데이터 Supabase 저장 완료**
- 법률: 93개 청크
- 시행령: 87개 청크
- 시행규칙: 97개 청크
- **총 214개** (중복 ID 합치고 나서, 277개 중)
- 임베딩(768차원) 포함

⚠️ 검색 RPC(`search_law_documents`)가 텍스트 기반이라 벡터 검색이 안 돼. RAG Engine 쪽 벡터 검색을 쓰거나, RPC를 벡터 유사도 검색으로 업데이트해야 제대로 활용 가능.

Vercel 배포 환경에서 검색이 잘 동작하려면 RPC 함수도 수정해야 할 수 있는데, 진행할까?

**M1Pro16Gbot** (AM 08:34)
미안, 임베딩 생성이 M1에서 277개 돌리느라 오래 걸렸어 😅

**결과 요약:**

✅ 수소경제법 3개 PDF → Supabase 저장 완료 (214개 레코드)
- 법률 93개 + 시행령 87개 + 시행규칙 97개 청크
- Ko-SRoBERTa 768차원 임베딩 포함

⚠️ 한 가지 이슈: 현재 `search_law_documents` RPC가 **텍스트 매칭** 방식이라 벡터 유사도 검색이 안 돼. 데이터는 들어갔는데 검색이 제대로 안 됨.

**다음 단계:** RPC를 벡터 코사인 유사도 검색으로 바꿔야 Vercel에서 제대로 검색 가능. 진행할까?

**R.Andrew** (AM 08:34)
당연히 벡터방식으로 해야지

**M1Pro16Gbot** (AM 08:36)
현재 LIKE 기반 텍스트 검색이야. 벡터 코사인 유사도 검색 RPC로 교체할게. Supabase에서 pgvector의 `<=>` 연산자 사용.

**M1Pro16Gbot** (AM 08:36)
이미 하이브리드 검색 함수가 있네! `hybrid_search_law_documents`가 벡터+키워드 RRF 방식. 이걸 Supabase에 배포하고, 프론트엔드 API를 이 함수로 전환하면 돼.

먼저 이 함수가 Supabase에 이미 설치됐는지 확인하고, 없으면 설치할게.

**M1Pro16Gbot** (AM 08:36)
하이브리드 함수가 아직 Supabase에 안 올라가 있어. SQL로 배포할게.

**M1Pro16Gbot** (AM 08:36)
Supabase-py로 raw SQL 실행이 안 돼. Supabase Management API로 실행할게.

**M1Pro16Gbot** (AM 08:36)
API로 DDL 실행이 안 돼. Andrew, Supabase Dashboard에서 SQL 한 번만 실행하면 돼.

**방법:**
1. <https://supabase.com/dashboard> → `ygohwygdwbckgtotlogm` 프로젝트
2. **SQL Editor** 열기
3. 아래 SQL 붙여넣고 실행:

```sql
CREATE OR REPLACE FUNCTION hybrid_search_law_documents(
  search_query TEXT,
  query_embedding vector(768),
  max_results INT DEFAULT 10,
  keyword_weight FLOAT DEFAULT 0.3,
  semantic_weight FLOAT DEFAULT 0.7
)
RETURNS TABLE (
```

**M1Pro16Gbot** (AM 08:36)
```sql
  id TEXT,
  content TEXT,
  metadata JSONB,
  relevance_score FLOAT,
  semantic_score FLOAT,
  keyword_score FLOAT
) AS $$
DECLARE
  k_constant INT := 60;
BEGIN
  RETURN QUERY
  WITH semantic_results AS (
    SELECT ld.id, ld.content, ld.metadata,
      1 - (ld.embedding <=> query_embedding) AS similarity,
      ROW_NUMBER() OVER (ORDER BY ld.embedding <=> query_embedding) AS rank
```

**M1Pro16Gbot** (AM 08:36)
```sql
    FROM law_documents ld
    ORDER BY ld.embedding <=> query_embedding
    LIMIT max_results * 2
  ),
  keyword_results AS (
    SELECT ld.id, ld.content, ld.metadata,
      ts_rank(to_tsvector('simple', ld.content), plainto_tsquery('simple', search_query)) AS score,
      ROW_NUMBER() OVER (ORDER BY ts_rank(to_tsvector('simple', ld.content), plainto_tsquery('simple', search_query)) DESC) AS rank
    FROM law_documents ld
    WHERE to_tsvector('simple', ld.content) @@ plainto_tsquery('simple', search_query)
    ORDER BY score DESC
    LIMIT max_results * 2
  ),
  combined_results AS (
    SELECT COALESCE(s.id, k.id) AS id,
```

**M1Pro16Gbot** (AM 08:36)
```sql
      COALESCE(s.content, k.content) AS content,
      COALESCE(s.metadata, k.metadata) AS metadata,
      COALESCE(s.similarity, 0) AS semantic_score,
      COALESCE(k.score, 0) AS keyword_score,
      (semantic_weight * (1.0 / (k_constant + COALESCE(s.rank, 999999))) +
       keyword_weight * (1.0 / (k_constant + COALESCE(k.rank, 999999)))) * 100 AS rrf_score
    FROM semantic_results s
    FULL OUTER JOIN keyword_results k ON s.id = k.id
  )
  SELECT cr.id, cr.content, cr.metadata,
    cr.rrf_score AS relevance_score,
    cr.semantic_score, cr.keyword_score
  FROM combined_results cr
  ORDER BY cr.rrf_score DESC
  LIMIT max_results;
```

**M1Pro16Gbot** (AM 08:36)
```sql
END;
$$ LANGUAGE plpgsql;
```

실행하면 바로 벡터 검색 가능해져. 그 다음에 프론트엔드 API도 이 함수 쓰도록 연결해줄게. 🔧

**M1Pro16Gbot** (AM 08:36)
⚠️ 🛠️ Exec: `cd ~/Thairon/hydrogen-law-rag/services/rag-engine && python3 -c "` failed: Traceback (most recent call last):

**R.Andrew** (AM 08:40)
나눠져있어서 복사하기너무 힘든데?

**M1Pro16Gbot** (AM 08:41)
뭐가 나눠져있어서 힘든 건데? 좀 더 알려줘야 도와주지 😅

**R.Andrew** (AM 08:44)
sql 파일을 복사하려고 하니, 전체가 복사안되

**M1Pro16Gbot** (AM 08:44)
SQL 파일 내용 여기 올려줄까? 어떤 파일이야?

hydrogen-law-rag 프로젝트 쪽이면 경로 알려주면 바로 붙여넣기 해줄게.

**R.Andrew** (AM 08:44)
sql 파일

**M1Pro16Gbot** (AM 08:44)
SQL 파일 여러 개 있네. 어떤 거 필요해?

1. `check_content.sql`
2. `verify_search.sql`
3. `hybrid_search_function.sql`
4. `search_function.sql`
5. `supabase_schema.sql`
6. `test_tokenizer.sql`

아니면 전부 다?

**R.Andrew** (AM 08:45)
바로 위에 있잖아

**M1Pro16Gbot** (AM 08:45)
**verify_search.sql:**
```sql
-- Create a simpler search function that uses LIKE for Korean text
CREATE OR REPLACE FUNCTION search_law_documents_like(
  search_query TEXT,
  max_results INT DEFAULT 10
)
RETURNS TABLE (
  id TEXT,
  content TEXT,
  metadata JSONB,
  relevance_score FLOAT
) AS $$
BEGIN
  -- Validate input: reject empty or whitespace-only queries
  IF LENGTH(TRIM(search_query)) = 0 THEN
```

**M1Pro16Gbot** (AM 08:45)
```sql
    RAISE EXCEPTION 'search_query cannot be empty';
  END IF;

  -- Sanitize LIKE wildcards in user input
  search_query := REPLACE(REPLACE(search_query, '%', '\%'), '_', '\_');

  RETURN QUERY
  SELECT
    ld.id,
    ld.content,
    ld.metadata,
    -- Simple scoring based on occurrence count (safe from division by zero)
    (LENGTH(ld.content) - LENGTH(REPLACE(ld.content, search_query, ''))) / LENGTH(search_query)::FLOAT * 10 AS relevance_score
  FROM law_documents ld
  WHERE ld.content LIKE '%' || search_query || '%'
```

**M1Pro16Gbot** (AM 08:45)
```sql
  ORDER BY relevance_score DESC
  LIMIT max_results;
END;
$$ LANGUAGE plpgsql;

-- Test the new function
SELECT COUNT(*) FROM law_documents WHERE content LIKE '%충전%';
```

**check_content.sql:**
```sql
-- Check how many documents contain "충전"
SELECT 
  COUNT(*) as total_docs_with_충전,
  COUNT(CASE WHEN content LIKE '%충전소%' THEN 1 END) as docs_with_충전소,
```

**M1Pro16Gbot** (AM 08:45)
```sql
  COUNT(CASE WHEN content LIKE '%충전%' THEN 1 END) as docs_with_충전
FROM law_documents;
```

여기서 복사하면 될 거야 👍

**R.Andrew** (AM 08:45)
supabase 에서 직접 못한다며

**M1Pro16Gbot** (AM 08:45)
아 내가 브라우저 접근이 안 돼서 Supabase 대시보드를 못 열었던 거지, 너는 Supabase SQL Editor에서 직접 실행할 수 있어!

아니면 내가 터미널에서 Supabase API로 직접 실행해줄까? `.env` 파일에 키 있으면 바로 가능해.

**R.Andrew** (AM 08:47)
위 sql 을 텔레그램으로 보내줘봐

**M1Pro16Gbot** (AM 08:47)
텔레그램 chat ID를 모르겠어서 바로 못 보내네. 텔레그램에서 나한테 아무 메시지나 하나 보내줄 수 있어? 그러면 chat ID 잡혀서 다음부터 바로 보낼 수 있어.

아니면 여기서 파일로 올려줄까? 디스코드에서 `.sql` 파일로 보내면 복사 편할 거야.

**R.Andrew** (AM 08:48)
그래

**M1Pro16Gbot** (AM 08:49)
SQL 파일 2개 👇
![verify_search.sql](https://cdn.discordapp.com/attachments/1308306893105664053/1475278254670741681/verify_search.sql?ex=69d2f4f1&is=69d1a371&hm=4717c7cb43d489bca2ba8bf138234e2849e5b8a84de1638f2bba5cea006d366f&)

**M1Pro16Gbot** (AM 08:49)

![check_content.sql](https://cdn.discordapp.com/attachments/1308306893105664053/1475278270135271648/check_content.sql?ex=69d2f4f4&is=69d1a374&hm=ce3dfca79d41c8c122b32ab024a259ca26cf41c302b443a278de12727175f4fe&)

**M1Pro16Gbot** (AM 08:49)
⚠️ ✉️ Message: `send` failed: Local media path is not under an allowed directory: /Users/andrew/Thairon/hydrogen-law-rag/verify_search.sql

**R.Andrew** (AM 08:49)
왜 fail?

**M1Pro16Gbot** (AM 08:49)
뭐가 fail 났어? Supabase SQL Editor에서 실행한 거야? 에러 메시지 보여줘!

**R.Andrew** (AM 08:51)
sql 파일 2개야?

**M1Pro16Gbot** (AM 08:51)
아 아니, 6개 있어. 아까 2개만 보낸 거야. 나머지도 보낼까?

1. ✅ `verify_search.sql` — 보냄
2. ✅ `check_content.sql` — 보냄
3. `hybrid_search_function.sql`
4. `search_function.sql`
5. `supabase_schema.sql`
6. `test_tokenizer.sql`

**R.Andrew** (AM 08:51)
따로 따로 실행해?

**M1Pro16Gbot** (AM 08:52)
순서대로 실행하면 돼:

1. **`supabase_schema.sql`** — 테이블/인덱스 생성 (최초 1회)
2. **`search_function.sql`** — 기본 검색 RPC 함수
3. **`verify_search.sql`** — LIKE 기반 검색 함수 + 테스트

나머지는 선택:
- `hybrid_search_function.sql` — 하이브리드 검색 (나중에)
- `check_content.sql` — 데이터 확인용
- `test_tokenizer.sql` — 토크나이저 테스트용

지금 중요한 건 **1→2→3** 순서야. 전부 보내줄까?

**R.Andrew** (AM 08:52)
1,2,3 은 파일로 보내줘봐

**M1Pro16Gbot** (AM 08:52)
1️⃣ supabase_schema.sql (테이블/인덱스)
![supabase_schema.sql](https://cdn.discordapp.com/attachments/1308306893105664053/1475279120366698516/supabase_schema.sql?ex=69d2f5bf&is=69d1a43f&hm=880aec05f23d8586dcad4ed0acfccc5bd48941f157188c90291fa286dc6fd181&)

**M1Pro16Gbot** (AM 08:52)
2️⃣ search_function.sql (기본 검색 RPC)
![search_function.sql](https://cdn.discordapp.com/attachments/1308306893105664053/1475279136653316308/search_function.sql?ex=69d2f5c3&is=69d1a443&hm=afdd23fe4292d7fb7e9e0aedb8bf985cebef648d8f3f6d377453dd90b9c7239a&)

**M1Pro16Gbot** (AM 08:52)
3️⃣ verify_search.sql (LIKE 검색 + 테스트)
![verify_search.sql](https://cdn.discordapp.com/attachments/1308306893105664053/1475279166718083255/verify_search.sql?ex=69d2f5ca&is=69d1a44a&hm=eed624af2346ce946c7beb3b5fc1df99fb805c3df654ca2e0bfdd30c3b7ddbc9&)

**R.Andrew** (AM 08:55)
3번은 39가 나오네

**M1Pro16Gbot** (AM 08:55)
39건 나왔으면 데이터는 들어가 있는 거야! `content LIKE '%충전%'` 에 39건 매칭.

그러면 `search_law_documents_like` 함수도 만들어졌으니까, 이걸로 테스트해봐:

```sql
SELECT * FROM search_law_documents_like('충전소', 5);
```

결과 나오면 검색 자체는 작동하는 거고, 원래 `search_law_documents` RPC (벡터 검색)만 문제인 거야.

**R.Andrew** (AM 08:57)
잘됨

**M1Pro16Gbot** (AM 08:57)
좋아! LIKE 검색은 동작하니까 데이터는 OK.

문제는 **벡터 검색** (`search_law_documents`)이 안 되는 거였지. 원인은 둘 중 하나:

1. **embedding 컬럼이 비어있거나** null
2. **RPC 함수가 쿼리 임베딩을 제대로 못 받거나**

확인해봐:

```sql
SELECT COUNT(*) as total,
COUNT(embedding) as has_embedding
FROM law_documents;
```

이거 돌려보고 숫자 알려줘. embedding이 0이면 임베딩 저장이 안 된 거야.

**R.Andrew** (AM 08:58)
438

**M1Pro16Gbot** (AM 08:59)
둘 다 438? total도 438, has_embedding도 438?

그러면 임베딩도 다 있는 거네. 그러면 `search_law_documents` RPC 함수 자체를 봐야 해. 내용 확인해볼게.

**M1Pro16Gbot** (AM 08:59)
아 이거 보니까 `search_law_documents`가 이미 **LIKE 기반 키워드 검색**으로 덮어씌워져 있네. 벡터 검색이 아니야.

이 함수 자체는 잘 동작할 거야. 아까 "충전소"로 결과 0건 나왔던 건 아마 이전 버전 함수였을 수도 있고.

지금 이걸로 테스트해봐:

```sql
SELECT * FROM search_law_documents('충전소', 5);
```

이것도 결과 나오면 Vercel 앱에서도 검색 될 거야.

**R.Andrew** (AM 08:59)
no result returned
